# Todo List - App Startup Fixes

- [x] Inspect and fix the `.venv` path verification in `start_app.sh`
- [ ] Verify that the backend successfully starts up and listens on port 8000
- [ ] Test the frontend in the browser to ensure the WebSocket connection is successful
